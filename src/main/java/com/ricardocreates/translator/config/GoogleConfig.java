package com.ricardocreates.translator.config;

import lombok.Builder;
import lombok.Data;

/**
 * Model of configuration file
 */
@Data
@Builder
public class GoogleConfig {
	//just in case we need extra configuration for google translator api
}
