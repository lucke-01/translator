implement new apis:
    see https://libretranslate.com/
    use feign to new implementation
compilation:
	compile copying and unzipping jdk with java fx
	compile copying script to be launched: .sh and .bat
	use of Launch4J to generate and exe app
Spring:
    see if it worth implement spring in the app
Improve performance:
	check if the text is the same in thread but parsing a trim in order to no reply request with final spaces
	use ehcache to no repeat requests

